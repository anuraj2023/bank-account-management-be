services:
  # ImmuDB service
  - type: pserv
    name: immudb
    runtime: image
    image:
      url: codenotary/immudb:latest
    disk:
      name: immudb-data
      mountPath: /var/lib/immudb

  # Web service
  - type: web
    name: app
    runtime: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    envVars:
      - key: SERVER_ADDRESS
      - key: IMMUDB_PORT
      - key: IMMUDB_ADDRESS
      - key: IMMUDB_USERNAME
      - key: IMMUDB_PASSWORD
    
  